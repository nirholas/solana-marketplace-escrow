{
  "name": "keyless-escrow-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Admin/moderator dashboard — moderators release escrow from the UI while the platform holds the keys and fires an atomic, master-funded release (the nirholas/atomic pattern).",
  "scripts": {
    "start": "node --experimental-strip-types src/server.ts",
    "dev": "node --experimental-strip-types --watch src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.0",
    "bs58": "^6.0.0",
    "express": "^4.21.2",
    "keyless-escrow": "*"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "license": "MIT"
}
