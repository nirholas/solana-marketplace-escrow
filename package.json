{
  "name": "solana-marketplace-escrow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Non-custodial Solana escrow SDK — a keyless vault where moderators can release funds but never steal them. Anchor PDA program, durable-nonce pre-signing, atomic Jito settlement. SDK + MCP + x402.",
  "workspaces": [
    "packages/*",
    "apps/*",
    "demo"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
